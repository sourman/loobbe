bool place_cell(unsigned int ID, struct Position *position)
{
	/* Normally we should check that this position is not taken up before
	 * we take it, but this function needsto be very fast due to the large
	 * number of cells, so we assume the algorithm doing the placement is
	 * not evil */
	this.position = position;
	return true;
}
